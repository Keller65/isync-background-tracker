<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <!-- El relay de tiempo real abre el socket desde el Service, sin pasar por JS. -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
  <!-- Geovalla de reposo (GeofencingClient) mientras el proceso está muerto,
       no solo dentro del Foreground Service. Pedida en dos pasos desde
       IsyncBackgroundLocationModule.requestPermissions() — Android 11+ lo exige. -->
  <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
  <!-- Notificación de entrada a una geovalla (canal `geovalla_events`), emitida
       desde el worker del Service — se postea en segundo plano sin JS. -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.VIBRATE" />

  <application>
    <service
      android:name=".IsyncBackgroundLocationService"
      android:exported="false"
      android:foregroundServiceType="location" />

    <receiver
      android:name=".BootReceiver"
      android:exported="true"
      android:enabled="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>

    <!-- Ambos reciben solo el PendingIntent que el propio Service arma, nunca
         un broadcast externo -> exported="false". -->
    <receiver android:name=".ActivityTransitionReceiver" android:exported="false" />
  </application>
</manifest>
